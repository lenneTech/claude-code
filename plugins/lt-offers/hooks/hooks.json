{
  "description": "Detects offer-related prompts to activate the creating-offers skill and names the MCP server (production or demo) the prompt should use",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/detect-offers-project.sh\"",
            "timeout": 5,
            "statusMessage": "Detecting offers context"
          }
        ]
      }
    ]
  }
}
